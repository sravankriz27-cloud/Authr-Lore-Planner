# GEMINI_API_KEY: Required for Gemini AI API calls.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this app is hosted.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# MONGODB_URI: Connection string for MongoDB database persistence (e.g., MongoDB Atlas).
# Replace <db_password> with your actual MongoDB user password.
# If left blank, the app will automatically fall back to standard local file storage (data/db.json).
MONGODB_URI="mongodb+srv://sravankriz27_db_user:<db_password>@lore-planner.oi55juh.mongodb.net/?appName=Lore-Planner"
